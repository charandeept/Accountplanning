kind: pipeline
type: docker

steps:
  - name: install_dependencies
    image: node:latest
    commands:
      - cd Account\ Planning/Web  # Specify the path to your project directory
      - npm install

  - name: build_project
    image: node:latest
    commands:
      - cd Account\ Planning/Web  # Specify the path to your project directory
      - npm run build
